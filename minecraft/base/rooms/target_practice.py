from village.building import Building, BuildingLayer, BuildingBlock, CompositeBuilding
from village.oriented_blocks import Torch
import mcpi.block as block
from mcpi.block import Block
from mcpi.vec3 import Vec3

class TargetPractice(Building):
	# TODO: large dark room in basement with arrow slits
	#		might want 2 levels with dispeners to flush down drops
	#       could add a 3rd channel to mob drop to drop into target practice room.
	pass

